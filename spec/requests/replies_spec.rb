require 'spec_helper'

describe "Replies" do
  describe "GET /replies" do
    it "works! (now write some real specs)" do
      get replies_path
      expect(response.status).to be(200)
    end
  end
end
